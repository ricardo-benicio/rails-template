# Development Procfile
# Run with: bin/dev
#
# Note: Tailwind CSS is compiled via Puma plugin (config/puma.rb)

web: bin/rails server -p ${PORT:-3000}

# Uncomment to enable Sidekiq (requires Redis running)
# jobs: bundle exec sidekiq -C config/sidekiq.yml
